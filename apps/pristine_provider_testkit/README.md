# Pristine Provider Testkit

`apps/pristine_provider_testkit` holds shared verification helpers for
downstream provider SDK repos.

The initial bootstrap keeps this app intentionally small and unpublished. It is
the workspace home for reusable artifact and conformance helpers that provider
repos can consume in `test`.
